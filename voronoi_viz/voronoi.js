class VoronoiT {
    constructor(points) {
        this.points = points;
    }

    delauney() {
        // return a list of triangle coords [ [(x,y), (x,y), (x,y)], ... ] for the trianglulation formulation
    }
}